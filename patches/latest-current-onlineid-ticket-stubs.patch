diff --git a/dlls/windows.security.authentication.onlineid/authenticator.c b/dlls/windows.security.authentication.onlineid/authenticator.c
index b781be2595..6828e25f56 100644
--- a/dlls/windows.security.authentication.onlineid/authenticator.c
+++ b/dlls/windows.security.authentication.onlineid/authenticator.c
@@ -119,8 +119,323 @@ struct authenticator
 {
     IOnlineIdSystemAuthenticatorForUser IOnlineIdSystemAuthenticatorForUser_iface;
     LONG ref;
+    GUID application_id;
 };
 
+struct ticket_async
+{
+    IAsyncOperation_OnlineIdSystemTicketResult IAsyncOperation_OnlineIdSystemTicketResult_iface;
+    IAsyncInfo IAsyncInfo_iface;
+    IOnlineIdSystemTicketResult IOnlineIdSystemTicketResult_iface;
+    LONG ref;
+};
+
+static inline struct ticket_async *impl_from_IAsyncOperation_OnlineIdSystemTicketResult(
+        IAsyncOperation_OnlineIdSystemTicketResult *iface )
+{
+    return CONTAINING_RECORD( iface, struct ticket_async, IAsyncOperation_OnlineIdSystemTicketResult_iface );
+}
+
+static inline struct ticket_async *impl_from_IAsyncInfo( IAsyncInfo *iface )
+{
+    return CONTAINING_RECORD( iface, struct ticket_async, IAsyncInfo_iface );
+}
+
+static inline struct ticket_async *impl_from_IOnlineIdSystemTicketResult( IOnlineIdSystemTicketResult *iface )
+{
+    return CONTAINING_RECORD( iface, struct ticket_async, IOnlineIdSystemTicketResult_iface );
+}
+
+static HRESULT WINAPI ticket_async_QueryInterface( IAsyncOperation_OnlineIdSystemTicketResult *iface, REFIID iid, void **out )
+{
+    struct ticket_async *impl = impl_from_IAsyncOperation_OnlineIdSystemTicketResult( iface );
+
+    TRACE( "iface %p, iid %s, out %p.\n", iface, debugstr_guid( iid ), out );
+
+    if (IsEqualGUID( iid, &IID_IUnknown ) ||
+        IsEqualGUID( iid, &IID_IInspectable ) ||
+        IsEqualGUID( iid, &IID_IAgileObject ) ||
+        IsEqualGUID( iid, &IID_IAsyncOperation_OnlineIdSystemTicketResult ))
+    {
+        *out = &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface;
+        IAsyncOperation_OnlineIdSystemTicketResult_AddRef( *out );
+        return S_OK;
+    }
+
+    if (IsEqualGUID( iid, &IID_IAsyncInfo ))
+    {
+        *out = &impl->IAsyncInfo_iface;
+        IAsyncInfo_AddRef( *out );
+        return S_OK;
+    }
+
+    FIXME( "%s not implemented, returning E_NOINTERFACE.\n", debugstr_guid( iid ) );
+    *out = NULL;
+    return E_NOINTERFACE;
+}
+
+static ULONG WINAPI ticket_async_AddRef( IAsyncOperation_OnlineIdSystemTicketResult *iface )
+{
+    struct ticket_async *impl = impl_from_IAsyncOperation_OnlineIdSystemTicketResult( iface );
+    return InterlockedIncrement( &impl->ref );
+}
+
+static ULONG WINAPI ticket_async_Release( IAsyncOperation_OnlineIdSystemTicketResult *iface )
+{
+    struct ticket_async *impl = impl_from_IAsyncOperation_OnlineIdSystemTicketResult( iface );
+    ULONG ref = InterlockedDecrement( &impl->ref );
+    if (!ref) free( impl );
+    return ref;
+}
+
+static HRESULT WINAPI ticket_async_GetIids( IAsyncOperation_OnlineIdSystemTicketResult *iface, ULONG *iid_count, IID **iids )
+{
+    FIXME( "iface %p, iid_count %p, iids %p stub!\n", iface, iid_count, iids );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_async_GetRuntimeClassName( IAsyncOperation_OnlineIdSystemTicketResult *iface, HSTRING *class_name )
+{
+    FIXME( "iface %p, class_name %p stub!\n", iface, class_name );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_async_GetTrustLevel( IAsyncOperation_OnlineIdSystemTicketResult *iface, TrustLevel *trust_level )
+{
+    if (!trust_level) return E_POINTER;
+    *trust_level = BaseTrust;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_put_Completed( IAsyncOperation_OnlineIdSystemTicketResult *iface,
+                                                  IAsyncOperationCompletedHandler_OnlineIdSystemTicketResult *handler )
+{
+    FIXME( "iface %p, handler %p semi-stub.\n", iface, handler );
+    if (handler) IAsyncOperationCompletedHandler_OnlineIdSystemTicketResult_Invoke( handler, iface, Completed );
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_get_Completed( IAsyncOperation_OnlineIdSystemTicketResult *iface,
+                                                  IAsyncOperationCompletedHandler_OnlineIdSystemTicketResult **handler )
+{
+    if (!handler) return E_POINTER;
+    *handler = NULL;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_GetResults( IAsyncOperation_OnlineIdSystemTicketResult *iface,
+                                               IOnlineIdSystemTicketResult **result )
+{
+    struct ticket_async *impl = impl_from_IAsyncOperation_OnlineIdSystemTicketResult( iface );
+
+    if (!result) return E_POINTER;
+    *result = &impl->IOnlineIdSystemTicketResult_iface;
+    IOnlineIdSystemTicketResult_AddRef( *result );
+    return S_OK;
+}
+
+static const struct IAsyncOperation_OnlineIdSystemTicketResultVtbl ticket_async_vtbl =
+{
+    ticket_async_QueryInterface,
+    ticket_async_AddRef,
+    ticket_async_Release,
+    /* IInspectable methods */
+    ticket_async_GetIids,
+    ticket_async_GetRuntimeClassName,
+    ticket_async_GetTrustLevel,
+    /* IAsyncOperation<OnlineIdSystemTicketResult *> methods */
+    ticket_async_put_Completed,
+    ticket_async_get_Completed,
+    ticket_async_GetResults,
+};
+
+static HRESULT WINAPI ticket_async_info_QueryInterface( IAsyncInfo *iface, REFIID iid, void **out )
+{
+    struct ticket_async *impl = impl_from_IAsyncInfo( iface );
+    return IAsyncOperation_OnlineIdSystemTicketResult_QueryInterface( &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface, iid, out );
+}
+
+static ULONG WINAPI ticket_async_info_AddRef( IAsyncInfo *iface )
+{
+    struct ticket_async *impl = impl_from_IAsyncInfo( iface );
+    return IAsyncOperation_OnlineIdSystemTicketResult_AddRef( &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface );
+}
+
+static ULONG WINAPI ticket_async_info_Release( IAsyncInfo *iface )
+{
+    struct ticket_async *impl = impl_from_IAsyncInfo( iface );
+    return IAsyncOperation_OnlineIdSystemTicketResult_Release( &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface );
+}
+
+static HRESULT WINAPI ticket_async_info_GetIids( IAsyncInfo *iface, ULONG *iid_count, IID **iids )
+{
+    FIXME( "iface %p, iid_count %p, iids %p stub!\n", iface, iid_count, iids );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_async_info_GetRuntimeClassName( IAsyncInfo *iface, HSTRING *class_name )
+{
+    FIXME( "iface %p, class_name %p stub!\n", iface, class_name );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_async_info_GetTrustLevel( IAsyncInfo *iface, TrustLevel *trust_level )
+{
+    if (!trust_level) return E_POINTER;
+    *trust_level = BaseTrust;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_info_get_Id( IAsyncInfo *iface, UINT32 *id )
+{
+    if (!id) return E_POINTER;
+    *id = 1;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_info_get_Status( IAsyncInfo *iface, AsyncStatus *status )
+{
+    if (!status) return E_POINTER;
+    *status = Completed;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_info_get_ErrorCode( IAsyncInfo *iface, HRESULT *error_code )
+{
+    if (!error_code) return E_POINTER;
+    *error_code = HRESULT_FROM_WIN32( ERROR_NOT_CONNECTED );
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_info_Cancel( IAsyncInfo *iface )
+{
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_async_info_Close( IAsyncInfo *iface )
+{
+    return S_OK;
+}
+
+static const struct IAsyncInfoVtbl ticket_async_info_vtbl =
+{
+    ticket_async_info_QueryInterface,
+    ticket_async_info_AddRef,
+    ticket_async_info_Release,
+    ticket_async_info_GetIids,
+    ticket_async_info_GetRuntimeClassName,
+    ticket_async_info_GetTrustLevel,
+    ticket_async_info_get_Id,
+    ticket_async_info_get_Status,
+    ticket_async_info_get_ErrorCode,
+    ticket_async_info_Cancel,
+    ticket_async_info_Close,
+};
+
+static HRESULT WINAPI ticket_result_QueryInterface( IOnlineIdSystemTicketResult *iface, REFIID iid, void **out )
+{
+    struct ticket_async *impl = impl_from_IOnlineIdSystemTicketResult( iface );
+
+    if (IsEqualGUID( iid, &IID_IUnknown ) ||
+        IsEqualGUID( iid, &IID_IInspectable ) ||
+        IsEqualGUID( iid, &IID_IAgileObject ) ||
+        IsEqualGUID( iid, &IID_IOnlineIdSystemTicketResult ))
+    {
+        *out = &impl->IOnlineIdSystemTicketResult_iface;
+        IOnlineIdSystemTicketResult_AddRef( *out );
+        return S_OK;
+    }
+
+    *out = NULL;
+    return E_NOINTERFACE;
+}
+
+static ULONG WINAPI ticket_result_AddRef( IOnlineIdSystemTicketResult *iface )
+{
+    struct ticket_async *impl = impl_from_IOnlineIdSystemTicketResult( iface );
+    return IAsyncOperation_OnlineIdSystemTicketResult_AddRef( &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface );
+}
+
+static ULONG WINAPI ticket_result_Release( IOnlineIdSystemTicketResult *iface )
+{
+    struct ticket_async *impl = impl_from_IOnlineIdSystemTicketResult( iface );
+    return IAsyncOperation_OnlineIdSystemTicketResult_Release( &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface );
+}
+
+static HRESULT WINAPI ticket_result_GetIids( IOnlineIdSystemTicketResult *iface, ULONG *iid_count, IID **iids )
+{
+    FIXME( "iface %p, iid_count %p, iids %p stub!\n", iface, iid_count, iids );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_result_GetRuntimeClassName( IOnlineIdSystemTicketResult *iface, HSTRING *class_name )
+{
+    if (!class_name) return E_POINTER;
+    return WindowsCreateString( RuntimeClass_Windows_Security_Authentication_OnlineId_OnlineIdSystemTicketResult,
+                                ARRAY_SIZE(RuntimeClass_Windows_Security_Authentication_OnlineId_OnlineIdSystemTicketResult) - 1,
+                                class_name );
+}
+
+static HRESULT WINAPI ticket_result_GetTrustLevel( IOnlineIdSystemTicketResult *iface, TrustLevel *trust_level )
+{
+    if (!trust_level) return E_POINTER;
+    *trust_level = BaseTrust;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_result_get_Identity( IOnlineIdSystemTicketResult *iface,
+                                                  __x_ABI_CWindows_CSecurity_CAuthentication_COnlineId_CIOnlineIdSystemIdentity **value )
+{
+    if (!value) return E_POINTER;
+    *value = NULL;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_result_get_Status( IOnlineIdSystemTicketResult *iface, OnlineIdSystemTicketStatus *value )
+{
+    if (!value) return E_POINTER;
+    *value = OnlineIdSystemTicketStatus_ServiceConnectionError;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_result_get_ExtendedError( IOnlineIdSystemTicketResult *iface, HRESULT *value )
+{
+    if (!value) return E_POINTER;
+    *value = HRESULT_FROM_WIN32( ERROR_NOT_CONNECTED );
+    return S_OK;
+}
+
+static const struct IOnlineIdSystemTicketResultVtbl ticket_result_vtbl =
+{
+    ticket_result_QueryInterface,
+    ticket_result_AddRef,
+    ticket_result_Release,
+    /* IInspectable methods */
+    ticket_result_GetIids,
+    ticket_result_GetRuntimeClassName,
+    ticket_result_GetTrustLevel,
+    /* IOnlineIdSystemTicketResult methods */
+    ticket_result_get_Identity,
+    ticket_result_get_Status,
+    ticket_result_get_ExtendedError,
+};
+
+static HRESULT create_completed_ticket_async( IAsyncOperation_OnlineIdSystemTicketResult **operation )
+{
+    struct ticket_async *impl;
+
+    if (!operation) return E_POINTER;
+    if (!(impl = calloc( 1, sizeof(*impl) ))) return E_OUTOFMEMORY;
+
+    impl->IAsyncOperation_OnlineIdSystemTicketResult_iface.lpVtbl = &ticket_async_vtbl;
+    impl->IAsyncInfo_iface.lpVtbl = &ticket_async_info_vtbl;
+    impl->IOnlineIdSystemTicketResult_iface.lpVtbl = &ticket_result_vtbl;
+    impl->ref = 1;
+
+    *operation = &impl->IAsyncOperation_OnlineIdSystemTicketResult_iface;
+    return S_OK;
+}
+
 static inline struct authenticator *impl_from_IOnlineIdSystemAuthenticatorForUser( IOnlineIdSystemAuthenticatorForUser *iface )
 {
     return CONTAINING_RECORD( iface, struct authenticator, IOnlineIdSystemAuthenticatorForUser_iface );
@@ -187,20 +502,26 @@ static HRESULT WINAPI authenticator_GetTrustLevel( IOnlineIdSystemAuthenticatorF
 static HRESULT WINAPI authenticator_GetTicketAsync( IOnlineIdSystemAuthenticatorForUser *iface, IOnlineIdServiceTicketRequest *request,
                                                     IAsyncOperation_OnlineIdSystemTicketResult **operation )
 {
-    FIXME( "iface %p, request %p, operation %p stub!\n", iface, request, operation );
-    return E_NOTIMPL;
+    FIXME( "iface %p, request %p, operation %p semi-stub returning service connection error.\n", iface, request, operation );
+    return create_completed_ticket_async( operation );
 }
 
 static HRESULT WINAPI authenticator_put_ApplicationId( IOnlineIdSystemAuthenticatorForUser *iface, GUID value )
 {
-    FIXME( "iface %p, value %s stub!\n", iface, debugstr_guid( &value ) );
-    return E_NOTIMPL;
+    struct authenticator *impl = impl_from_IOnlineIdSystemAuthenticatorForUser( iface );
+
+    FIXME( "iface %p, value %s semi-stub.\n", iface, debugstr_guid( &value ) );
+    impl->application_id = value;
+    return S_OK;
 }
 
 static HRESULT WINAPI authenticator_get_ApplicationId( IOnlineIdSystemAuthenticatorForUser *iface, GUID *value )
 {
-    FIXME( "iface %p, value %p stub!\n", iface, value );
-    return E_NOTIMPL;
+    struct authenticator *impl = impl_from_IOnlineIdSystemAuthenticatorForUser( iface );
+
+    if (!value) return E_POINTER;
+    *value = impl->application_id;
+    return S_OK;
 }
 
 static HRESULT WINAPI authenticator_get_User( IOnlineIdSystemAuthenticatorForUser *iface, __x_ABI_CWindows_CSystem_CIUser **user )
diff --git a/dlls/windows.security.authentication.onlineid/ticket.c b/dlls/windows.security.authentication.onlineid/ticket.c
index c162f5dfe5..e1e2e1e09f 100644
--- a/dlls/windows.security.authentication.onlineid/ticket.c
+++ b/dlls/windows.security.authentication.onlineid/ticket.c
@@ -29,6 +29,14 @@ struct ticket_request_factory_statics
     LONG ref;
 };
 
+struct ticket_request
+{
+    IOnlineIdServiceTicketRequest IOnlineIdServiceTicketRequest_iface;
+    LONG ref;
+    HSTRING service;
+    HSTRING policy;
+};
+
 static inline struct ticket_request_factory_statics *impl_from_IActivationFactory( IActivationFactory *iface )
 {
     return CONTAINING_RECORD( iface, struct ticket_request_factory_statics, IActivationFactory_iface );
@@ -114,20 +122,149 @@ static const struct IActivationFactoryVtbl factory_vtbl =
     factory_ActivateInstance,
 };
 
+static inline struct ticket_request *impl_from_IOnlineIdServiceTicketRequest( IOnlineIdServiceTicketRequest *iface )
+{
+    return CONTAINING_RECORD( iface, struct ticket_request, IOnlineIdServiceTicketRequest_iface );
+}
+
+static HRESULT WINAPI ticket_request_QueryInterface( IOnlineIdServiceTicketRequest *iface, REFIID iid, void **out )
+{
+    struct ticket_request *impl = impl_from_IOnlineIdServiceTicketRequest( iface );
+
+    TRACE( "iface %p, iid %s, out %p.\n", iface, debugstr_guid( iid ), out );
+
+    if (IsEqualGUID( iid, &IID_IUnknown ) ||
+        IsEqualGUID( iid, &IID_IInspectable ) ||
+        IsEqualGUID( iid, &IID_IAgileObject ) ||
+        IsEqualGUID( iid, &IID_IOnlineIdServiceTicketRequest ))
+    {
+        *out = &impl->IOnlineIdServiceTicketRequest_iface;
+        IOnlineIdServiceTicketRequest_AddRef( *out );
+        return S_OK;
+    }
+
+    FIXME( "%s not implemented, returning E_NOINTERFACE.\n", debugstr_guid( iid ) );
+    *out = NULL;
+    return E_NOINTERFACE;
+}
+
+static ULONG WINAPI ticket_request_AddRef( IOnlineIdServiceTicketRequest *iface )
+{
+    struct ticket_request *impl = impl_from_IOnlineIdServiceTicketRequest( iface );
+    ULONG ref = InterlockedIncrement( &impl->ref );
+    TRACE( "iface %p increasing refcount to %lu.\n", iface, ref );
+    return ref;
+}
+
+static ULONG WINAPI ticket_request_Release( IOnlineIdServiceTicketRequest *iface )
+{
+    struct ticket_request *impl = impl_from_IOnlineIdServiceTicketRequest( iface );
+    ULONG ref = InterlockedDecrement( &impl->ref );
+
+    TRACE( "iface %p decreasing refcount to %lu.\n", iface, ref );
+
+    if (!ref)
+    {
+        WindowsDeleteString( impl->service );
+        WindowsDeleteString( impl->policy );
+        free( impl );
+    }
+    return ref;
+}
+
+static HRESULT WINAPI ticket_request_GetIids( IOnlineIdServiceTicketRequest *iface, ULONG *iid_count, IID **iids )
+{
+    FIXME( "iface %p, iid_count %p, iids %p stub!\n", iface, iid_count, iids );
+    return E_NOTIMPL;
+}
+
+static HRESULT WINAPI ticket_request_GetRuntimeClassName( IOnlineIdServiceTicketRequest *iface, HSTRING *class_name )
+{
+    if (!class_name) return E_POINTER;
+    return WindowsCreateString( RuntimeClass_Windows_Security_Authentication_OnlineId_OnlineIdServiceTicketRequest,
+                                ARRAY_SIZE(RuntimeClass_Windows_Security_Authentication_OnlineId_OnlineIdServiceTicketRequest) - 1,
+                                class_name );
+}
+
+static HRESULT WINAPI ticket_request_GetTrustLevel( IOnlineIdServiceTicketRequest *iface, TrustLevel *trust_level )
+{
+    if (!trust_level) return E_POINTER;
+    *trust_level = BaseTrust;
+    return S_OK;
+}
+
+static HRESULT WINAPI ticket_request_get_Service( IOnlineIdServiceTicketRequest *iface, HSTRING *value )
+{
+    struct ticket_request *impl = impl_from_IOnlineIdServiceTicketRequest( iface );
+    if (!value) return E_POINTER;
+    return WindowsDuplicateString( impl->service, value );
+}
+
+static HRESULT WINAPI ticket_request_get_Policy( IOnlineIdServiceTicketRequest *iface, HSTRING *value )
+{
+    struct ticket_request *impl = impl_from_IOnlineIdServiceTicketRequest( iface );
+    if (!value) return E_POINTER;
+    return WindowsDuplicateString( impl->policy, value );
+}
+
+static const struct IOnlineIdServiceTicketRequestVtbl ticket_request_vtbl =
+{
+    ticket_request_QueryInterface,
+    ticket_request_AddRef,
+    ticket_request_Release,
+    /* IInspectable methods */
+    ticket_request_GetIids,
+    ticket_request_GetRuntimeClassName,
+    ticket_request_GetTrustLevel,
+    /* IOnlineIdServiceTicketRequest methods */
+    ticket_request_get_Service,
+    ticket_request_get_Policy,
+};
+
+static HRESULT create_ticket_request( HSTRING service, HSTRING policy, IOnlineIdServiceTicketRequest **request )
+{
+    struct ticket_request *impl;
+    HRESULT hr;
+
+    if (!request) return E_POINTER;
+    *request = NULL;
+    if (!(impl = calloc( 1, sizeof(*impl) ))) return E_OUTOFMEMORY;
+
+    impl->IOnlineIdServiceTicketRequest_iface.lpVtbl = &ticket_request_vtbl;
+    impl->ref = 1;
+
+    if (FAILED( hr = WindowsDuplicateString( service, &impl->service ) ) ||
+        FAILED( hr = WindowsDuplicateString( policy, &impl->policy ) ))
+    {
+        IOnlineIdServiceTicketRequest_Release( &impl->IOnlineIdServiceTicketRequest_iface );
+        return hr;
+    }
+
+    *request = &impl->IOnlineIdServiceTicketRequest_iface;
+    return S_OK;
+}
+
 DEFINE_IINSPECTABLE( ticket_request_factory_statics, IOnlineIdServiceTicketRequestFactory, struct ticket_request_factory_statics, IActivationFactory_iface )
 
 static HRESULT WINAPI ticket_request_factory_statics_CreateOnlineIdServiceTicketRequest( IOnlineIdServiceTicketRequestFactory *iface, HSTRING service,
                                                                                          HSTRING policy, IOnlineIdServiceTicketRequest **request )
 {
-    FIXME( "iface %p, service %s, policy %s, request %p stub!\n", iface, debugstr_hstring( service ), debugstr_hstring( policy ), request );
-    return E_NOTIMPL;
+    FIXME( "iface %p, service %s, policy %s, request %p semi-stub.\n", iface, debugstr_hstring( service ), debugstr_hstring( policy ), request );
+    return create_ticket_request( service, policy, request );
 }
 
 static HRESULT WINAPI ticket_request_factory_statics_CreateOnlineIdServiceTicketRequestAdvanced( IOnlineIdServiceTicketRequestFactory *iface, HSTRING service,
                                                                                                  IOnlineIdServiceTicketRequest **request )
 {
-    FIXME( "iface %p, service %s, request %p stub!\n", iface, debugstr_hstring( service ), request );
-    return E_NOTIMPL;
+    HSTRING policy;
+    HRESULT hr;
+
+    FIXME( "iface %p, service %s, request %p semi-stub.\n", iface, debugstr_hstring( service ), request );
+
+    if (FAILED( hr = WindowsCreateString( L"", 0, &policy ) )) return hr;
+    hr = create_ticket_request( service, policy, request );
+    WindowsDeleteString( policy );
+    return hr;
 }
 
 static const struct IOnlineIdServiceTicketRequestFactoryVtbl ticket_request_factory_statics_vtbl =
